@model DivinationGenerator.Models.FactionViewModel

@{
    ViewData["Title"] = "Divinations";
}

<h1>Divinations: @Html.DisplayFor(model => model.name)</h1>

<div>
    @{
        Random random = new Random();
        <div>
        <b>@Model.divinations[random.Next(0, Model.divinations.Count - 1)];</b>
        </div>
        <div>
        <b>@Model.divinations[random.Next(0, Model.divinations.Count - 1)];</b>
        </div>
        <div>
        <b>@Model.divinations[random.Next(0, Model.divinations.Count - 1)];</b>
        </div>
    }
</div>




<div style="margin-top:75px">
    <a asp-action="Index">Home</a>
</div>
